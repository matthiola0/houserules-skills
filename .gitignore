# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (if any tooling is added later)
node_modules/

# This repo dogfoods its own house rules: .ai-team/ config files are
# committed as a live example. Only the generated artifacts are ignored.
.ai-team/reviews/
.ai-team/tests/
